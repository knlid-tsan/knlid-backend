export enum RewardStatus {
  PENDING = 'pending',
  AWAITING_PAYMENT = 'awaiting_payment',
  PAID = 'paid',
  OVERDUE = 'overdue',
  PAID_BY_GUARANTOR = 'paid_by_guarantor',
  DISPUTED = 'disputed',
  CANCELLED = 'cancelled',
}
